# Article Worker Reports

This directory is for review-only reports generated by Article Worker.

Reports are not articles. Do not publish them directly.

Generate a report with:

```bash
cd workers/article-worker
npm run plan -- --topic "AI API cost optimization"
```

Use reports as editorial planning material before creating or improving drafts.
